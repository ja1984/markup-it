/** @jsx h */
import h from '../../hyperscript';

// The <User> React Element is stripped because it's not valid HTML
export default (
    <document>
        <paragraph>Hi, my name is John</paragraph>
    </document>
);
