/** @jsx h */
import h from '../../hyperscript';

export default (
    <document>
        <paragraph>
            This is <strikethrough>striked</strikethrough> text
        </paragraph>
    </document>
);
