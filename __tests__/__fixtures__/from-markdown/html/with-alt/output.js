/** @jsx h */
import h from '../../../hyperscript';

export default (
    <document>
        <paragraph>
            Some image with attributes{' '}
            <image src="images/foo.png" title="foo" />
        </paragraph>
    </document>
);
