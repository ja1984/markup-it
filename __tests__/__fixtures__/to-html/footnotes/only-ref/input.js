/** @jsx h */
import h from '../../../hyperscript';

export default (
    <document>
        <paragraph>
            This is a footnote
            <footnote-ref id={2} />
        </paragraph>
    </document>
);
