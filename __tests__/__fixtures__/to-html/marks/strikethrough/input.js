/** @jsx h */
import h from '../../../hyperscript';

export default (
    <document>
        <paragraph>
            Hello <strikethrough>World</strikethrough>
        </paragraph>
    </document>
);
