nodes:
  - kind: block
    type: code_block
    nodes:
      - kind: block
        type: code_line
        nodes:
          - kind: text
            text: "foreach (Employee emp in result)"
      - kind: block
        type: code_line
        nodes:
          - kind: text
            text: "{"
      - kind: block
        type: code_line
        nodes:
          - kind: text
            text: '  Console.WriteLine(emp.FirstName + " " + emp.LastName);'
      - kind: block
        type: code_line
        nodes:
          - kind: text
            text: "}"
