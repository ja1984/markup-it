/** @jsx h */
import h from 'h';

export default (
    <document>
        <paragraph>
            <inline type="math" isVoid data={{ formula: 'a = b' }} /> world
        </paragraph>
    </document>
);
